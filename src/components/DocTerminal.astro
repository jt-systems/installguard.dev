---
import Terminal from "./Terminal.astro";

interface Props {
  title?: string;
  body: string;
}

const { title, body } = Astro.props;
---

<div class="not-content">
  <Terminal title={title} body={body} />
</div>
